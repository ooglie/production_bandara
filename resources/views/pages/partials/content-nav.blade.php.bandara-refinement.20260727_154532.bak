@php
    $items = [
        ['route' => 'content.about', 'label' => 'About Bandara'],
        ['route' => 'content.help', 'label' => 'Help & FAQs'],
        ['route' => 'content.terms', 'label' => 'Terms & Policies'],
        ['route' => 'content.privacy', 'label' => 'Privacy'],
    ];
@endphp

<nav aria-label="Bandara information pages" class="border-b border-slate-200/80 bg-white/80 dark:border-slate-800 dark:bg-slate-950/75">
    <div class="mx-auto flex max-w-7xl gap-1 overflow-x-auto px-4 py-2 sm:px-6 lg:px-8">
        @foreach ($items as $item)
            <a
                href="{{ route($item['route']) }}"
                @class([
                    'shrink-0 rounded-md px-3 py-2 text-xs font-normal transition sm:text-sm',
                    'bg-slate-900 text-white dark:bg-slate-100 dark:text-slate-950' => request()->routeIs($item['route']),
                    'text-slate-600 hover:bg-slate-100 hover:text-slate-950 dark:text-slate-300 dark:hover:bg-slate-900 dark:hover:text-white' => ! request()->routeIs($item['route']),
                ])
                @if (request()->routeIs($item['route'])) aria-current="page" @endif
            >
                {{ $item['label'] }}
            </a>
        @endforeach
    </div>
</nav>
